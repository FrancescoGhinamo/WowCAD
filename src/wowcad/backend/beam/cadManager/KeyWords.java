package wowcad.backend.beam.cadManager;

/**
 * Fixed key words that compose the command
 * @author franc
 *
 */
public class KeyWords {

	/**
	 * Reference to point
	 */
	public static final String POINT = "POINT";
	
	/*
	 * continuare in modo analogo con i nomi delle primitive e dei vari possibili parametri dei comandi (che non devono cambiare)
	 * da fare sicuramente:
	 * circle
	 * ellipse
	 * polygon
	 * polyline
	 * segment
	 * save_on_disk
	 * 
	 * (per esportazione)
	 * true
	 * false
	 */
	
}
